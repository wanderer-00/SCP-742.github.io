let cluster = document.getElementById('CIRCLE');

cluster.onclick = function LITE_DARK(event){
    var target = event.target;
    if (target.tagName != 'div'){
        alert('da')
    }
};
//    if (event.style.color = `var(--BG)`){
//        event.style.color = `var(--CL)`;   
//    } else {
//        event.style.color = `var(--BG)`;
//    }
